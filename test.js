declare global { interface Window { AWEB_PARAMS: { language: string; }; } } function changeLang(engine: EngineContext, $: JQueryStatic): void { let htmlLang = document.documentElement.lang; let hideLangSwitcher = false; let updatedLang; let searchParams = new URLSearchParams(window.location.search)    if (searchParams.has('lang')) { let param = searchParams.get('lang')         if (param === "AR") { updatedLang = 'ar-jo'; }//more languages here         else {            updatedLang = 'en-US';        }    } else if (htmlLang === 'AR') {        updatedLang = 'ar-jo';    } else {        updatedLang = 'en-US';    }    window.AWEB_PARAMS.language = updatedLang;     if (hideLangSwitcher) {        engine.event(function (content, $) {            $("li.mk-aweb-menu-lang").css("display", "none");        }, EngineState.Build);    }}

