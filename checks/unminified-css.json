{
    "id": "unminified-css",
    "evaluate": "unminified-css.js",
    "metadata": {
        "impact": "moderate",
        "messages": {
            "pass": "Potential savings with minifying CSS are insignificant",
            "fail": "You loose potential savings with avoiding minifying CSS. {{=it.data.url}} has {{=Number((it.data.wastedBytes / 1024).toFixed(1)).toLocaleString('en-En')}} KB wasted and {{=it.data.wastedPercent.toFixed(1)}} wasted percent. {{=it.data.displayValue}} of all css."
        }
    }
}