{
    "id": "aria-required-children",
    "evaluate": "required-children.js",
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "Required ARIA children are present",
            "fail": "Required ARIA {{=it.data.description && it.data.description.length > 1 ? 'children' : 'child'}} role not present:{{~it.data.description:value}} {{=value}}{{~}}"
        }
    }
}