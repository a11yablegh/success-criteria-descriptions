{
    "id": "aria-valid-attr",
    "evaluate": "valid-attr.js",
    "options": {},
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "ARIA attribute name{{=it.data.description && it.data.description.length > 1 ? 's' : ''}} are valid",
            "fail": "Invalid ARIA attribute name{{=it.data.description && it.data.description.length > 1 ? 's' : ''}}:{{~it.data.description:value}} {{=value}}{{~}}"
        }
    }
}