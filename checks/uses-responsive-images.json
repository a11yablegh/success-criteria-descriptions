{
    "id": "uses-responsive-images",
    "evaluate": "uses-responsive-images.js",
    "metadata": {
        "impact": "moderate",
        "messages": {
            "pass": "Your images have proper size",
            "fail": "Rendered size of image is at least 25KB smaller than the actual size. {{=it.data.url}} has {{=Number((it.data.wastedBytes / 1024).toFixed(1)).toLocaleString('en-En')}} KB wasted and {{=it.data.wastedPercent.toFixed(1)}} wasted percent. {{=it.data.displayValue}} of all images."
        }
    }
}