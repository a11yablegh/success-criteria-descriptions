{
    "id": "aria-errormessage",
    "evaluate": "errormessage.js",
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "Uses a supported aria-errormessage technique",
            "fail": "aria-errormessage value{{=it.data.errormessageData && it.data.errormessageData.length > 1 ? 's' : ''}} {{~it.data.errormessageData:value}} `{{=value}}{{~}}` must use a technique to announce the message (e.g., aria-live, aria-describedby, role=alert, etc.)"
        }
    }
}