{
    "id": "aria-required-attr",
    "evaluate": "required-attr.js",
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "All required ARIA attributes are present",
            "fail": "Required ARIA attribute{{=it.data.description && it.data.description.length > 1 ? 's' : ''}} not present:{{~it.data.description:value}} {{=value}}{{~}}"
        }
    }
}