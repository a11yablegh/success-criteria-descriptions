{
    "id": "aria-required-parent",
    "evaluate": "required-parent.js",
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "Required ARIA parent role present",
            "fail": "Required ARIA parent{{=it.data.description && it.data.description.length > 1 ? 's' : ''}} role not present:{{~it.data.description:value}} {{=value}}{{~}}"
        }
    }
}