{
    "id": "aria-valid-attr-value",
    "evaluate": "valid-attr-value.js",
    "options": {},
    "metadata": {
        "impact": "critical",
        "messages": {
            "pass": "ARIA attribute values are valid",
            "fail": "Invalid ARIA attribute value{{=it.data.validAttrValueData && it.data.validAttrValueData.invalid && it.data.validAttrValueData.invalid.length > 1 ? 's' : ''}}:{{~it.data.validAttrValueData.invalid:value}} {{=value}}{{~}}"
        }
    }
}